<%= form_for(@acquereur) do |f| %>
  <% if @acquereur.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@acquereur.errors.count, "error") %> prohibited this acquereur from being saved:</h2>

      <ul>
      <% @acquereur.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :Nom %><br>
    <%= f.text_field :nom %>
  </div>
  <div class="field">
    <%= f.label :Prenom %><br>
    <%= f.text_field :prenom %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
